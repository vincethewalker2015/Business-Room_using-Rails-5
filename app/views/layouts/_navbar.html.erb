<nav class="navbar navbar-inverse navbar-static-top" role="navigation">
  <div class='container'>
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main-nav-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: "navbar-brand navbar-left" do %>
        <i class="fa fa-briefcase"></i><span> Business Den</span>
      <% end %>
    </div>
    <div class="collapse navbar-collapse" id="main-nav-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "btn btn-default copper" %></li>
        <% else %>
          <li><%= link_to "Log in", new_user_session_path, class: "btn btn-default copper" %></li>
        <% end %>
        <li><%= link_to "About", about_path %></li>
        <li><%= link_to "All Users", users_path if user_signed_in? %></li>
        <li><%= link_to "Edit your profile", edit_user_profile_path(user_id: current_user.id) if user_signed_in? %></li>
        <li><%= link_to "View your profile", user_path(current_user) if user_signed_in? %></li>
        <li><%= link_to "Get in Contact", contact_us_path %></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>